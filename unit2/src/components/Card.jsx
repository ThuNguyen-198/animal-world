import React from "react";
const Card = (props) => {
  return (
    <div></div>
    // <div className={"Card " + props.level}>
    //   <img className="animal-img" src={props.img} alt={props.name} />
    //   {/* <p className="animal-name">{props.name}</p> */}
    // </div>
  );
};
export default Card;
